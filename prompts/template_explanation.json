{
  "name": "template_explanation",
  "version": "2026-04-13",
  "description": "Default template prompt for creating explanation prompts.",
  "mode": "explanation",
  "system_prompt": "You are a language-learning translator. Output a JSON array only. Do not output explanations or code fences.",
  "user_prompt_template": "source_lang={source_lang}\ntarget_lang={target_lang}\ndocument_title={document_title}\nsource_url={source_url}\n\n请将 `text_originals_json` 中的每一项翻译为目标语言。\n输入是 JSON 数组，输出也必须是 JSON 数组，且长度必须与 {batch_size} 完全一致，并保持原顺序。\n\n输出格式（严格）：\n[\n  {\"translation\": \"...\"},\n  {\"translation\": \"...\"}\n]\n\n要求：\n1) 必须按位置一一对应，不要合并、拆分或重排条目。\n2) 译文自然、地道。\n3) 不要添加如 \"Translation:\" 的前缀。\n4) 可以使用 <b>...</b> 强调；不要使用 Markdown **。\n5) 如果某一项无法翻译，请对该项返回 {\"translation\": \"\"}，其余项继续正常输出。\n6) 只返回 JSON，不要解释文字，不要代码块。\n\ntext_originals_json:\n{text_originals_json}\n\n下面的 `chunk_text` 仅用于消歧；不要整体翻译它：\n{chunk_text}",
  "placeholders": [
    "source_lang",
    "target_lang",
    "document_title",
    "source_url",
    "chunk_text",
    "text_original",
    "text_originals_json",
    "batch_size"
  ],
  "output_format": {
    "type": "json",
    "schema_name": "translation_cards_v1"
  },
  "parser": {
    "strip_code_fences": true,
    "expect_json_array": true
  },
  "content_type": "all",
  "learning_mode": "all",
  "difficulty_level": "all"
}
