services:
  clawlearn-web:
    build: .
    container_name: clawlearn-web
    env_file:
      - .env
    ports:
      - "7860:7860"
    volumes:
      - ./runs:/app/runs
      - ./outputs:/app/outputs
      - ./logs:/app/logs
    restart: unless-stopped
