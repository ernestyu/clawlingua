"""Web UI for ClawLingua (Gradio-based, optional).

This package provides a small local-only HTTP frontend for ClawLingua.
It is intentionally separate from the core CLI so that the CLI behavior
remains stable and minimal.
"""
